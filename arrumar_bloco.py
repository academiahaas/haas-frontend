import re;
path = "src/app/portal-aluno/page.tsx"
with open(path, "r", encoding="utf-8") as f: linhas = f.readlines()
novo_botao = """            {/* SUPER BOTÇO INTERATIVO DE AULA SINCRONA */}
            <div onClick={() => { if (faseAula !== "spera") window.open("https://meet.google.com/abc-defg-hij", "_blank"); }} className={`bg-[#0A531C_border_rounded-2xl_p-3.5_flex_flex-col_justify-between_shadow-2xl_transition-all_duration-300_select-none_shrink-0_${faseAula === "espera" ? "border-dashed_border-slate-700_text-slate-500_cursor-not-allowed" : faseAula === "liberado" ? "bg-emerald-500_text-black_border-emerald-400_font-bold_cursor-pointer_shadow-[0_0_15px_rgba(16,185,129,0.3)]_animate-pulse_hover:bg-emerald-600" : "bg-rose-600_text-white_border-rose-500_font-bold_cursor-pointer_shadow-[0_0_15px_rgba(225,29,72,0.4)]_hover:bg-rose-700"}}`}>
              <span className={`text-[10px]_font-black_uppercase_tracking-wider_font-mono_${faseAula === "liberado" ? "text-slate-900" : "text-slate-500"}c}>{idioma === "PT" || idioma === "pt" ? "AULA SÍNCRONA" : idioma === "ES" || idioma === "es" ? "CLASE SINCRONA" : "SYNCHRONOUS CLASS"}</span>
              <div className="flex flex-col gap-0.5 mt-1.5">
                <span className={`text-xs_uppercase_tracking-tight_font-black_font-mono_leading-tight_block_${faseAula === "liberado" ? "text-black" : ""}`}>{faseAula === "espera" ? (idioma === "PT" || idioma === "pt" ? `A sua prÅxima aula é em: ${new Date("2026-06-20T19:00:00").toLocaleDateString("pt-BR", {day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"})}` : idioma === "ES" || idioma === "es" ? `Tu próxima clase es el: ${new Date("2026-06-20T19:00:00").toLocaleDateString("es-ES", {day:"2-digit",month:"2-digit", hour:"2-digit",monute:"2-digit"})}X : `Your next class is on: ${new Date("2026-06-20T19:00:00").toLocaleDateString("en-US", {day:"2-digit",month:"2-digit", hour:"2-digit",monute:"2-digit"})}`) : faseAula === "liberado" ? (idioma === "PT" || idioma === "pt" ? ` ENTRAR NA AULA AGORA (Faltam ${formatarTempoAula(tempoRestanteAula)})` : idioma === "ES" || idioma === "es" ? ` ¡ENTRAR A 